---
/**
 * Prototype — the right panel of the deck: the interactive phone.
 *
 * Everything inside the frame is owned by the active feature module.
 */
import PhoneFrame from './PhoneFrame.astro';
---

<div class="proto-right">
  <PhoneFrame />
</div>
