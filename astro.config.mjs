// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://lttlandromeda.github.io',
  base: '/rymax-support-prototype',
});
